<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Test Uppy Uploader</title>

  <!-- Uppy CSS -->
  <link
    href="https://releases.transloadit.com/uppy/v4.18.0/uppy.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      font-family: Georgia, serif;
      background: #fdfdfd;
      padding: 2rem;
      text-align: center;
    }

    #files-drag-drop {
      max-width: 640px;
      margin: 2rem auto;
      padding: 1rem;
      border-radius: 12px;
      background: #fff;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.06);
      border: 1px solid #eaeaea;
    }

    h1 {
      font-size: 1.6rem;
      margin-bottom: 0.5rem;
    }

    p {
      color: #555;
      margin-bottom: 1rem;
      font-size: 1rem;
    }
  </style>
</head>
<body>
  <h1>Test Upload Dashboard 💌</h1>
  <p>Drop a file to confirm the uploader loads correctly.</p>

  <div id="files-drag-drop"></div>

  <script type="module">
    import {
      Uppy,
      Dashboard,
    } from 'https://releases.transloadit.com/uppy/v4.18.0/uppy.min.mjs'

    const uppy = new Uppy()

    uppy.use(Dashboard, {
      target: '#files-drag-drop',
      inline: true,
      note: 'This is a test—no files will be uploaded',
      proudlyDisplayPoweredByUppy: false,
    })

    uppy.on('file-added', (file) => {
      console.log('File added:', file)
      alert(`File "${file.name}" added. Upload not wired yet.`)
    })
  </script>
</body>
</html>
